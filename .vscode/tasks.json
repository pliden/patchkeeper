{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Cargo build",
            "command": "cargo", // command "cargo build" did not work for me
            "args": ["build"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
